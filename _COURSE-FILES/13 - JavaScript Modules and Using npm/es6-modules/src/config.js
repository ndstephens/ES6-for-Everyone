export const myApiKey = 'xyz789'
export const url = 'http://fivefloral.com'
export function sayHi(name) {
  console.log(`Hello there ${name}`)
}

const apiKey = 'abc123'
export default apiKey

const age = 41
const cat = 'taco'

export { age, cat }

// can either 'export' item by item, or all at once at the bottom of the file in one export statement
